name: Release Packages

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 2.1.0 without v prefix)'
        required: true
        type: string

jobs:
  # Extract version and set as environment variable
  setup:
    name: Setup Version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - name: Get version
        id: get_version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="${{ github.event.release.tag_name }}"
            # Strip 'v' prefix if present
            VERSION="${VERSION#v}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Using version: $VERSION"

  # Build data files first
  build:
    name: Build Data
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm install --ignore-scripts
      
      - name: Build data files
        run: npm run build
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: generated-data
          path: |
            libs/javascript/data/
            libs/python/creditcard_identifier/brands.py
            libs/python/creditcard_identifier/brands_detailed.py
            libs/ruby/lib/creditcard_identifier/brands.rb
            libs/ruby/lib/creditcard_identifier/brands_detailed.rb
            libs/elixir/lib/creditcard_identifier/data.ex
            libs/elixir/lib/creditcard_identifier/data_detailed.ex
            libs/dotnet/CreditCardIdentifier/BrandData.cs
            libs/dotnet/CreditCardIdentifier/BrandDataDetailed.cs

  # Publish to npm
  publish-npm:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: [setup, build]
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        working-directory: libs/javascript
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'
      
      - name: Update version
        run: npm version ${{ needs.setup.outputs.version }} --no-git-tag-version --allow-same-version
      
      - name: Publish to npm with provenance
        run: npm publish --access public --provenance

  # Publish to PyPI
  publish-pypi:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [setup, build]
    permissions:
      id-token: write
    defaults:
      run:
        working-directory: libs/python
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install build tools
        run: pip install build
      
      - name: Update version
        run: |
          sed -i "s/version = \".*\"/version = \"${{ needs.setup.outputs.version }}\"/" pyproject.toml
      
      - name: Build package
        run: python -m build
      
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: libs/python/dist/

  # Publish to RubyGems
  publish-rubygems:
    name: Publish to RubyGems
    runs-on: ubuntu-latest
    needs: [setup, build]
    defaults:
      run:
        working-directory: libs/ruby
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
      
      - name: Update version
        run: |
          sed -i "s/spec.version.*=.*/spec.version       = '${{ needs.setup.outputs.version }}'/" creditcard-identifier.gemspec
      
      - name: Build gem
        run: gem build creditcard-identifier.gemspec
      
      - name: Publish to RubyGems
        run: |
          mkdir -p ~/.gem
          echo -e "---\n:rubygems_api_key: ${{ secrets.RUBYGEMS_API_KEY }}" > ~/.gem/credentials
          chmod 0600 ~/.gem/credentials
          gem push creditcard-identifier-${{ needs.setup.outputs.version }}.gem
        env:
          GEM_HOST_API_KEY: ${{ secrets.RUBYGEMS_API_KEY }}

  # Publish to Hex.pm
  publish-hex:
    name: Publish to Hex.pm
    runs-on: ubuntu-latest
    needs: [setup, build]
    defaults:
      run:
        working-directory: libs/elixir
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Setup Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: '1.15'
          otp-version: '26'
      
      - name: Update version
        run: |
          sed -i "s/@version \".*\"/@version \"${{ needs.setup.outputs.version }}\"/" mix.exs
      
      - name: Install dependencies
        run: mix deps.get
      
      - name: Publish to Hex.pm
        run: mix hex.publish --yes
        env:
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}

  # Publish to NuGet
  publish-nuget:
    name: Publish to NuGet
    runs-on: ubuntu-latest
    needs: [setup, build]
    defaults:
      run:
        working-directory: libs/dotnet
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      
      - name: Update version
        run: |
          sed -i "s/<Version>.*<\/Version>/<Version>${{ needs.setup.outputs.version }}<\/Version>/" CreditCardIdentifier/CreditCardIdentifier.csproj
      
      - name: Pack NuGet package
        run: dotnet pack CreditCardIdentifier/CreditCardIdentifier.csproj -c Release -o ./nupkg
      
      - name: Publish to NuGet
        run: dotnet nuget push ./nupkg/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate

  # Publish Go Module
  publish-go:
    name: Publish Go Module
    runs-on: ubuntu-latest
    needs: [setup, build]
    permissions:
      contents: write
    defaults:
      run:
        working-directory: libs/go
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      
      - name: Run tests
        run: go test -v
      
      - name: Create module tag
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git tag "libs/go/v${{ needs.setup.outputs.version }}"
          git push origin "libs/go/v${{ needs.setup.outputs.version }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Trigger pkg.go.dev update
        run: |
          curl "https://proxy.golang.org/github.com/renatovico/bin-cc/libs/go/v2/@v/v${{ needs.setup.outputs.version }}.info"

  # Publish to Maven Central
  publish-maven:
    name: Publish to Maven Central
    runs-on: ubuntu-latest
    needs: [setup, build]
    defaults:
      run:
        working-directory: libs/java
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
      
      - name: Update version
        run: |
          # Only update the project version (line after artifactId), not plugin versions
          sed -i '0,/<artifactId>creditcard-identifier<\/artifactId>/{ n; s/<version>.*<\/version>/<version>${{ needs.setup.outputs.version }}<\/version>/; }' pom.xml
      
      - name: Build with Maven
        run: mvn clean package
      
      - name: Publish to Maven Central
        run: mvn deploy -P release
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_CENTRAL_TOKEN }}

  # Publish to crates.io
  publish-crates:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    needs: [setup, build]
    defaults:
      run:
        working-directory: libs/rust
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Update version
        run: |
          sed -i "s/^version = \".*\"/version = \"${{ needs.setup.outputs.version }}\"/" Cargo.toml
      
      - name: Run tests
        run: cargo test --verbose
      
      - name: Publish to crates.io
        run: cargo publish --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

  # Publish to Packagist
  publish-packagist:
    name: Publish to Packagist
    runs-on: ubuntu-latest
    needs: [setup, build]
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: generated-data
          path: libs/
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          tools: composer
      
      - name: Update version in root composer.json
        run: |
          sed -i "s/\"version\": \".*\"/\"version\": \"${{ needs.setup.outputs.version }}\"/" composer.json
      
      - name: Update version in libs/php/composer.json
        working-directory: libs/php
        run: |
          sed -i "s/\"version\": \".*\"/\"version\": \"${{ needs.setup.outputs.version }}\"/" composer.json
      
      - name: Install dependencies
        working-directory: libs/php
        run: composer install
      
      - name: Run tests
        working-directory: libs/php
        run: vendor/bin/phpunit
      
      - name: Commit and tag version
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add composer.json libs/php/composer.json
          git commit -m "chore(php): bump version to ${{ needs.setup.outputs.version }}" || true
          git push origin master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Trigger Packagist update
        if: github.repository == 'renatovico/bin-cc'
        continue-on-error: true
        run: |
          curl -X POST "https://packagist.org/api/update-package" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.PACKAGIST_USERNAME }}:${{ secrets.PACKAGIST_TOKEN }}" \
            -d '{"repository":"https://github.com/renatovico/bin-cc"}'
